rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
  
  	match /{user}/{uid} {
    	allow read, write;
    }
    
    match /{document=**} {
      allow read, write: if true;
    }
  }
}